gradio>=5.0
huggingface_hub>=0.20.0
# Install the project (cs336_basics + scripts packages) from GitHub so
# the Space has the playground UI and model code without duplicating it.
# Brings in torch, numpy, regex, einops, pyyaml, tqdm as transitive deps.
cs336-basics @ git+https://github.com/Qwerty0826/char-level-transformer-lm.git@main
